# Production Environment Configuration
# This file is used when building for production (npm run build)

# Use mock data in production
VITE_USE_MOCK_DATA=true

# API Configuration (fallback, not used in mock mode)
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_ROOT_URL=https://your-production-domain.com

# App Configuration
VITE_APP_TITLE=SeniorVibe ILS Survey
VITE_APP_VERSION=0.1.0
